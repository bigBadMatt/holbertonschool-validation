build:
	hugo

clean:
	rm -r dist

post:
	hugo new posts/$(POST_NAME).md
	awk -v title="$POST_TITLE" '/^title:/{gsub(/$(POST_NAME)/, title)}1' content/posts/"$POST_NAME".md > content/posts/"$POST_NAME".tmp && mv content/posts/"$POST_NAME".tmp content/posts/"$POST_NAME".md
